import React from 'react';
import styles from './style.module.less';

export const Notification = () => {
  return (
    <div className={styles["Notification"]}>This is the Notification's page</div>
  );
};
